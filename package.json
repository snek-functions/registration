{
  "name": "@snek-functions/registration",
  "version": "0.0.1",
  "private": false,
  "main": "dist/index.js",
  "type": "module",
  "description": "Generated with `snek-functions init`",
  "author": "snek-at",
  "dependencies": {
    "@snek-at/functions": "*",
    "@snek-functions/iam": "*"
  },
  "devDependencies": {
    "nodemon": "^2.0.19",
    "serverless": "^3.19.0",
    "serverless-offline": "^9.2.6",
    "serverless-plugin-ifelse": "^1.0.7"
  },
  "scripts": {
    "build": "sf build -f .",
    "start": "sf build -f . && IS_OFFLINE=true nodemon --watch dist --exec sls offline --httpPort 4030 --lambdaPort 4032",
    "deploy": "sls deploy",
    "postinstall": "sf install"
  },
  "sfDependencies": {}
}
